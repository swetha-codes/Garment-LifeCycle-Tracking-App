# Garment-LifeCycle-Tracking-App
The app is a simple, privacy‑preserving companion UI that shows per‑garment usage summaries (e.g., wears, washes, last seen) generated locally by the Raspberry Pi, and lets staff control what data is stored and displayed via local privacy settings (no tracking).
